@Library('shared-libraries')_
pipeline {
    agent any
    tools {
        maven "Maven"   
    }   
  
    stages {
        stage('Compile-Build-Test ') {
            steps {
	    	slacknotification()
                build 'BUILD'
           	    }
        	}	
      }
     }
