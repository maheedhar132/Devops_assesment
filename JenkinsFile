@Library('shared-libraries')_
pipeline {
    agent any
    tools {
        maven "Maven"   
    }   
  
    stages {
        stage('Compile-Build-Test ') {
            steps {
	    	slacknotification 'Build Success'
                build 'BUILD'
           	    }
		post
		{
		 failure { jira 'Failed in Build Stage'}
		}
            }	
	
      }
     }
