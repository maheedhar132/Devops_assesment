@Library('shared-libraries')_
pipeline {
    agent any
    tools {
        maven "Maven"   
    }   
  
    stages {
        stage('Compile-Build-Test ') {
            steps {
	    	slacknotification '#faea0a'
                build 'BUILD'
           	    }
        	}	
	post{
		success{slacknotification '#46f202'}
		}	
      }
     }
